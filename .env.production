# =============================================================================
# GK-Nexus Production Environment Configuration
# =============================================================================
# Copy this file to .env.production and fill in the values
# IMPORTANT: Never commit .env.production to version control
# =============================================================================

# -----------------------------------------------------------------------------
# PostgreSQL Database
# -----------------------------------------------------------------------------
# Used by both docker-compose and the application
POSTGRES_DB=synergy_gy
POSTGRES_USER=postgres
POSTGRES_PASSWORD=nsn1QL3L2xuiReQNurMwkAZbmo9kJpom

# Port to expose PostgreSQL (internal use only, leave as 5432 unless conflicting)
POSTGRES_PORT=5432

# -----------------------------------------------------------------------------
# Application Settings
# -----------------------------------------------------------------------------
# Port to expose the application
APP_PORT=8843

# Better Auth secret - REQUIRED
# Generate with: openssl rand -base64 32
BETTER_AUTH_SECRET=LB+AgEM+ELe4LLLglHMqsBxrkqhuBtzFn2qqYc9orHg=

# Base URL for Better Auth
# In production, this should match your domain
BETTER_AUTH_URL=http://localhost:3000

# CORS origin for frontend
# Use * for any origin, or specify your frontend domain
CORS_ORIGIN=*

# -----------------------------------------------------------------------------
# Frontend Configuration (build-time)
# -----------------------------------------------------------------------------
# API URL for the frontend to connect to
# This is baked into the frontend at build time
VITE_SERVER_URL=http://localhost:3000

# -----------------------------------------------------------------------------
# Email Configuration (Optional)
# -----------------------------------------------------------------------------
# Resend API key for sending emails
# Get your key at https://resend.com
RESEND_API_KEY=

# From email address (must be verified in Resend)
EMAIL_FROM=noreply@yourdomain.com

# -----------------------------------------------------------------------------
# Cloud Backup Configuration (Optional)
# -----------------------------------------------------------------------------
# S3-compatible storage for offsite backups
# Works with AWS S3, Cloudflare R2, MinIO, etc.

# Cloudflare R2 example:
# BACKUP_S3_ENDPOINT=https://YOUR_ACCOUNT_ID.r2.cloudflarestorage.com

# AWS S3 example:
# BACKUP_S3_ENDPOINT=https://s3.amazonaws.com

BACKUP_S3_ENDPOINT=
BACKUP_S3_ACCESS_KEY_ID=
BACKUP_S3_SECRET_ACCESS_KEY=
BACKUP_S3_BUCKET=gk-nexus-backups
BACKUP_S3_REGION=auto

# -----------------------------------------------------------------------------
# Initial Admin Setup (First Run Only)
# -----------------------------------------------------------------------------
# These are used to create the first admin account
# Only used if no users exist in the database
# Can be removed after initial setup
INITIAL_ADMIN_EMAIL=admin@gcmc-kaj.com
INITIAL_ADMIN_PASSWORD=Admin@1qazxsw2
INITIAL_ADMIN_NAME=System Administrator

# =============================================================================
# Quick Start
# =============================================================================
#
# 1. Copy this file: cp .env.production.example .env.production
# 2. Edit .env.production and fill in all CHANGE_ME values
# 3. Generate secrets:
#    - POSTGRES_PASSWORD: openssl rand -base64 24
#    - BETTER_AUTH_SECRET: openssl rand -base64 32
# 4. Start the stack: docker-compose -f docker-compose.prod.yml up -d
# 5. Run migrations: docker-compose -f docker-compose.prod.yml exec app bun run db:migrate
# 6. Access at http://localhost:3000 (or your configured APP_PORT)
#
# =============================================================================
